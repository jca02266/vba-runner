# Documentation Update Log

* **Initialization**: Added an OKF-compatible index and metadata to the core
  testing and refactoring guides.
* **Preservation**: Renamed the previous detailed index to
  `legacy-index.md` so the OKF-reserved lowercase `index.md` can be used.
* **Validation**: Added repository validation for OKF frontmatter and local
  Markdown links, exposed as `npm run check:docs`.
* **Provenance**: Added generation, verification, source, and freshness
  metadata to the documentation hub and core guides.
