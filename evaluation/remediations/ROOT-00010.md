---
id: ROOT-00010
procedureVersion: 1
status: cancelled
priority: high
title: Registry・Interaction入力契約と戻り配列形状を共通化する
rootCauseId: RC-00023
causeKey: builtin-boundary-contract-dispersion
originEvaluations:
  - EV-00267
originFindings:
  - BUG-00253
  - BUG-00254
  - BUG-00255
scope:
  include:
    - AppActivate/SendKeysの文形式Wait引数を共通必須契約へ接続する
    - GetAllSettingsの空セクションと2次元配列形状メタデータを共通表現で扱う
    - Registry・InteractionのNull、Missing、空文字の入口差を回帰で固定する
  exclude:
    - 実Excel固有の文形式Waitエラー番号
    - Registryの保存先仕様そのもの
    - 他のCOMメソッドの引数契約
acceptanceCriteria:
  - 文形式Wait:=NullがVBAの期待エラー境界で処理される
  - 空セクションのGetAllSettingsが仕様どおりの配列境界を返す
  - Registry・Interactionの既存3Finding回帰が通過する
  - 必須String変換と戻り配列形状の共通契約を別実装で重複させない
tests:
  - tests/spec/registry.test.ts
  - tests/spec/interaction.test.ts
  - tests/spec/array-subscript-bounds.test.ts
---

# Registry・Interaction契約の対処タスク

RC-00023で確定した組み込み境界の分散に対し、未照合の文形式Waitと空Registry境界を、
既存の必須String変換および多次元配列表現へ接続する。実Excel固有のエラー番号は、
実機照合後に実装範囲を確定する。

RCA-FOLLOWUP-001/002で必要な境界を個別に評価し、Interaction文形式はROOT-00013で対処済み、
空Registry配列はバグなしと確認したため、この重複タスクは実装せず取り下げる。
