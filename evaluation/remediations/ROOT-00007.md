---
id: ROOT-00007
procedureVersion: 1
status: completed
priority: high
title: As Object配列引数のコンテナ形状を検証する
rootCauseId: RC-00007
causeKey: array-parameter-container-validation-gap
originEvaluations:
  - EV-00403
originFindings:
  - BUG-00403
scope:
  include:
    - 通常Class/Module手続きのAs Object()配列引数へ渡す値の配列コンテナ検証
    - スカラー値を本体へ渡さない共通binder境界
    - Object()配列とNothing要素の回帰
  exclude:
    - Object()/NamedClass()の要素型厳密一致
    - ByVal配列宣言の静的禁止
    - Property、CallByName、COMの別引数契約
acceptanceCriteria:
  - As Object()へスカラー値を渡すと本体へ到達せず配列引数エラーになる
  - Object()配列は受理され、Nothing要素を保持できる
  - ModuleとClassの共通binder経路で同じコンテナ検証を行う
  - 要素型互換性を別候補として残す
tests:
  - tests/spec/class-module.test.ts
commit: '7c861c9'
verification:
  - tests/spec/class-module.test.tsのAs Object()コンテナ境界回帰が通過した
  - npm run typecheck --if-present が通過した
  - scripts/bg-test.shでTypeScript 354件、VBA 111件がすべて通過した
---

# As Object()配列引数のコンテナ形状検証

RC-00007で確定した範囲に対し、配列指定子付きObject引数のコンテナ形状だけを検証する。
配列要素の型互換性やProperty/CallByName固有の入口は別評価で扱う。
