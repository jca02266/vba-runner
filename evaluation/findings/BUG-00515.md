---
id: BUG-00515
findingRecordVersion: 2
evaluation: EV-00820
discoveredBy:
  - EV-00820
status: fixed
discoveryType: excel-comparison
area: ファイルI/O・FSO
directCauseKey: com-member-omitted-call-arity-bypass
causeKey: dynamic-member-arity-contract-not-shared
rootCauseProcedureVersion: 1
directFixStatus: fixed
rootFixStatus: unassessed
directFixCommit: ebeb65ca
horizontalAudit:
  status: completed
  confirmed:
    - FSO.GetFileのSet省略・括弧付き省略・Variant省略がExcelのError 450と一致しない
    - TextStream.Readで確認済みの動的メンバー必須引数差異と同じ観測範囲にある
  ruledOut: []
  unresolved:
    - Dictionary.Exists、Collection.Itemなど他の動的メンバーへの適用範囲
rootCauseAnalysis:
  status: hypothesis
  rootCauseId: RC-00070
regression:
  status: passed
  tests:
    - tests/spec/missing-arg.test.ts
followUpDisposition: registered
followUpCandidates:
  - FZ-COVERAGE-20260810-320
---

# FSO.GetFileの省略引数がExcelのError 450にならない

## 事象

必須pathnameを取る`FSO.GetFile`を省略形式または括弧付き省略形式で呼び出すと、
ExcelはSet代入・値代入ともError 450を返す。Runnerは代入形式ごとに424、449、または
エラーなしとなり、エラー番号とエラー段階が一致しない。

## 横展開

TextStream.Readで確認済みの動的メンバー必須引数差異と同じメンバー評価・代入境界を
確認した。Dictionary.ExistsとCollection.Itemは別候補で確認する。

## 真因分析

直接原因は、動的メンバーの省略呼び出しが代入形式ごとの経路に分岐し、共通の必須引数
契約を適用していないことである。設計上の真因はRC-00070の仮説として保持し、他の
動的メンバーの横展開で共通化範囲を確定する。

## 回帰テスト

`tests/spec/missing-arg.test.ts`でFSO.GetFileのSet/Variant、裸参照/括弧付き省略を
回帰確認済みである。
