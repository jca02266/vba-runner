---
id: BUG-00617
findingRecordVersion: 2
title: 修飾UDT配列をByRef配列引数へ渡すと型不一致になる
status: fixed
discoveryType: coverage
evaluation: EV-01011
discoveredBy:
  - EV-01011
candidateId: RCA-PRIVATE-VISIBILITY-021
directCauseKey: qualified-udt-array-element-identity-mismatch
causeKey: type-identity-canonicalization-missing
directFixStatus: fixed
rootFixStatus: planned
commit: f72abab8
horizontalAudit:
  status: completed
  confirmed:
    - Public Module.Type配列を同じPublic UDTの裸名ByRef配列引数へ渡すとError 13になる
    - 配列生成は完了し、失敗箇所はByRef配列コンテナ検査である
  ruledOut:
    - Parserが修飾型配列を拒否すること
    - UDT配列の初期化自体が失敗すること
    - スカラーByRef型検査だけが入口であること
  unresolved:
    - Property Let、Function/Property配列戻り値、ReDim後の同一不一致
    - 同名UDTの所有モジュール優先順位
rootCauseAnalysis:
  status: hypothesis
  procedureVersion: 1
  rootCauseId: RC-00124
  directCause: runtime配列要素型と宣言配列要素型を生の文字列で比較し、修飾名と裸名を正規化しない
  designCause: 型の意味的所有者を含むcanonical identityを全型経路で共有していない
  confirmed:
    - validateArrayParameterContainerが今回のエラーを送出する
    - 配列メタデータに修飾名が保持され、callee側は裸名で比較される
  ruledOut:
    - UDTパーサー・配列実体化の失敗
  unresolved:
    - 配列戻り値・Property Let・ReDim経路への横展開
  decision: RC-00124のcanonical UDT identity対処タスク候補として継続する
regression:
  status: passed
  tests:
    - tests/spec/private-type-declaration.test.ts
followUpDisposition: registered
followUpCandidates:
  - RCA-FOLLOWUP-064
---

## 事象

`Producer.RecordT`として宣言した配列を、`RecordT`要素型のByRef配列引数へ渡すと、同じPublic UDTにもかかわらずRunnerが実行時エラー13を返す。[EV-01011](../evaluations/EV-01011.md)で再現した。

## 横展開

配列宣言とUDT実体化は成功し、配列ByRefコンテナ検査だけが修飾名と裸名を不一致として扱った。Property・配列戻り値・ReDim後の経路は未確認である。

## 真因分析

直接原因候補は配列要素型メタデータの生文字列比較である。設計上の真因仮説は、所有モジュールを含むcanonical UDT identityが型生成・ByRef検査・戻り値経路で共有されていないこと。RC-00124に記録し、独立レビュー後に確定する。

## 回帰テスト

`tests/spec/private-type-declaration.test.ts`で、修飾/裸名のPublic UDT配列ByRefが48を返し、所有者の異なる同名UDTはType mismatchとなることを検証した。Property・戻り値・ReDim後の追加経路はRC-00124の未解決項目として継続する。
