---
id: EV-00854
evaluationRecordVersion: 2
candidateId: FZ-COVERAGE-20260810-340
campaign: FZ-COVERAGE-20260810
status: fixed
priority: 1
focus: 引数・ByRef・Property領域のCallByName経由Scalar ByRef型不一致と書戻し
area: "引数・ByRef・Property"
areaSource: inferred
expectation:
  kind: excel
  statement: CallByNameでByRef LongへInteger変数を渡した場合のExcelの型検査と書戻し契約を確定する
  references:
    - source: https://learn.microsoft.com/ja-jp/office/vba/language/reference/user-interface-help/byref-argument-type-mismatch
      ref: ByRef引数は期待型と一致する必要がある
    - source: docs/implementation/VBA_SPEC_LIST.md
      ref: 動的呼出しにおけるByRef型契約
  verification: completed
directFixStatus: fixed
rootFixStatus: planned
commit: 0dedf6dd
findings:
  - BUG-00535
tests:
  - tests/spec/callbyname-byref-type.test.ts
  - tests/excel/queue/ExcelQueueVerification.bas
excelProbeIds:
  - XL-210
coverageSnapshot: COV-2026-08-14-current
---

## 評価内容

クラスの`ByRef Long`メソッドを`CallByName`で呼び出し、`Integer`変数を渡した場合の
型不一致検査と呼出し元への書戻しを、通常の直接メンバー呼出しと比較する。

## 実施方法

次の最小ソースをランナーとExcelで実行する。

```vba
Class Worker
    Public Sub TakeLong(ByRef value As Long)
        value = value + 1
    End Sub
End Class

Sub Probe()
    Dim worker As New Worker, value As Integer
    value = 10
    On Error Resume Next
    Err.Clear: CallByName worker, "TakeLong", VbMethod, value
    Debug.Print "ERR=" & CStr(Err.Number) & " VALUE=" & CStr(value)
End Sub
```

## 期待値

期待出力：

```text
ERR=13 VALUE=10
```

Excelの`Err.Number`と`value`を記録し、直接メンバー呼出しのByRef型検査と同じ契約か、
CallByNameのVariant配列による動的呼出し契約として暗黙変換を許すかを確定する。

## 結果

実測出力：

```text
ERR=0 VALUE=11
```

文章による結果：ランナーのCallByName経路は異型Integerを受理し、Longへ変換した値を
呼出し元へ書き戻した（修正前）。一方、Excelは型不一致を報告し、呼出し元の値を
変更しなかった。

Excel実測出力：

```text
XL-210 CALLBYNAME ERR=13 VALUE=10
```

修正後のランナー出力：

```text
ERR=13 VALUE=10
```

## 判定

判定状態: fixed

ExcelはCallByName経由でもByRefの実引数型を検査してError 13を返す。ランナーは
修正前は異型Integerを受理して値を書き戻していた。クラス呼出しを共通ByRefバインダーへ
接続し、修正後はError 13と値不変でExcelに一致した。
