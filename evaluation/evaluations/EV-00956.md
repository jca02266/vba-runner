---
id: EV-00956
evaluationRecordVersion: 2
candidateId: META-DECLARATION-ORDER-20260904-002
campaign: META-DECLARATION-ORDER-20260904
status: needs-excel
priority: 2
focus: Enumモジュール変数の宣言順不変性
area: "宣言・型・リテラル"
areaSource: confirmed
expectation:
  kind: hypothesis
  statement: Enum型のモジュール変数は、Enum宣言の前後にかかわらず同じ既定値とCStr変換結果になる
  references:
    - source: spec/MS-VBAL.txt
      ref: "§5.2.3.1, §5.2.3.3, §5.6.16.7"
  verification: pending
excelProbeIds:
  - XL-252
tests:
  - tests/excel/queue/ExcelQueueVerification.bas
  - tests/spec/module-udt-declaration-order.test.ts
findings: []
coverageSnapshot: COV-2026-08-14-current
---

## 評価内容

Enum型を参照するモジュール変数について、Enum宣言の前後で既定値と`CStr`変換結果が変わらないかを比較する。

## 実施方法

`ExcelQueueVerification.bas`の`XL-252`で、Enum宣言より前の`enumBefore`と、Enum宣言より後の`enumAfter`を同じ手続きから読み取る。各値について`VarType`、変換時の`Err.Number`、変換文字列を出力する。vba-runnerでも同じソースを実行し、Excel結果と比較する。

## 期待値

仮説上の期待出力は、前後の変数で`VarType`、エラー番号、`CStr`結果が一致することである。具体的な値はExcel実機結果を根拠として確定する。

## 結果

vba-runnerでは前後とも`VarType=3`だが、既定値の`CStr`でError 91になる。Excel実機結果は未取得である。

## 判定

判定状態: needs-excel

Excel実機でEnumの既定値と変換結果を確認するまで、仕様差またはバグとは確定しない。
