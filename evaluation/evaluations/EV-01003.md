---
id: EV-01003
evaluationRecordVersion: 2
candidateId: RCA-PRIVATE-VISIBILITY-013
campaign: FZ-COVERAGE-20260823
status: fixed
priority: 3
focus: Event宣言とRaiseEventの引数個数不一致
area: 制御フロー
areaSource: confirmed
expectation:
  kind: spec
  statement: RaiseEventの引数リストは対象Event宣言の引数契約と一致し、不一致は静的に拒否される
  references:
    - source: ../../spec/MS-VBAL.txt
      ref: 5.4.2.20 RaiseEvent statement and procedure invocation argument compatibility
  verification: completed
findings:
  - BUG-00611
directCauseKey: event-contract-precheck-missing
causeKey: event-contract-not-centralized
tests:
  - tests/spec/raiseevent.test.ts
commit: d27e7fea
---

## 評価内容

Event宣言とRaiseEvent発火側の引数個数が一致しない場合に、Runnerが静的診断するかを確認する。

## 実施方法

```vba
' Source.cls
Option Explicit
Public Event Ping(ByVal firstValue As Long, ByVal secondValue As Long)

Public Sub Fire()
    RaiseEvent Ping(1)
End Sub

' Module1.bas
Option Explicit
Public Function Caller() As Long
    Dim source As New Source
    source.Fire
    Caller = 1
End Function
```

クラスをロードして診断を取得し、`Caller`を実行する。引数を2個にした一致対照も確認する。

## 期待値

プログラム上の期待出力: `RaiseEvent Ping(1)`は宣言の2引数契約に違反するためコンパイル診断となり、`Caller`は実行されない。

文章での期待: RaiseEventは対象Eventの位置引数リストと互換でなければならず、引数個数不一致はイベント発火前の静的検査で拒否される。

## 結果

プログラム上の実測出力: 修正前はRunnerのロードが成功し診断は`[]`、`Caller`は`1`を返した。修正後はRaiseEvent実行時に`Compile error: Wrong number of arguments for RaiseEvent`となる。

文章での結果: `evaluateRaiseEventStatement`がEvent宣言を解決せず、引数個数を比較しないため、仕様上不正な発火を実行可能として扱っている。

## 判定

判定状態: fixed。

独立評価はハンドラー不在でも引数過少RaiseEventが無診断で通ることを再現した。Event宣言の必須・最大引数数を共有precheckで検査する修正と回帰テストにより不一致を拒否することを確認した。ハンドラー署名・Interface・ホスト固有のタイミングは別候補として追跡する。
