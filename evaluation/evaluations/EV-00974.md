---
id: EV-00974
evaluationRecordVersion: 2
candidateId: FZ-COVERAGE-20260823-017
campaign: FZ-COVERAGE-20260823
status: bug-found
priority: 1
focus: For Each配列制御変数のVariant静的要件
area: 制御フロー
areaSource: confirmed
expectation:
  kind: spec
  statement: 配列を列挙するFor Eachの制御変数はVariantでなければならず、Longでは実行前にコンパイルエラーになる
  references:
    - source: spec/MS-VBAL.txt
      ref: 5.4.2.4 Static Semantics - declared type of bound-variable-expression MUST be Variant when collection is an array
  verification: completed
findings:
  - BUG-00593
tests:
  - tests/spec/for-each.test.ts
coverageSnapshot: COV-2026-09-07-foreach-control-coercion
rootCauseProcedureVersion: 1
directCauseKey: foreach-array-control-type-precheck-missing
causeKey: foreach-static-semantics-not-modeled
rootCauseId: RC-00115
directFixStatus: fixed
rootFixStatus: in-progress
---

## 評価内容

配列を列挙するFor Eachで、Long型の制御変数を許容していないか確認する。

## 実施方法

```vba
Function InvalidArrayControl() As Long
    Dim values(0 To 1) As Long
    Dim value As Long
    For Each value In values
        InvalidArrayControl = InvalidArrayControl + value
    Next value
End Function
```

## 期待値

プログラム上の期待出力: `InvalidArrayControl`の呼び出しはコンパイルエラーになる。
文章での期待: MS-VBAL 5.4.2.4の静的意味論は、配列のFor Each制御変数の宣言型をVariantに限定しているため、Long制御変数は実行に進まず拒否される。独立した仕様レビューでもこの読み取りを確認した。

## 結果

プログラム上の実測出力: 修正前のRunnerは`3`を返し、修正後は`Compile error: For Each control variable on arrays must be Variant`を返す。
文章での結果: 修正前は静的要件を検査せず実行していた。静的検査を追加したことで、対象ソースを実行前に拒否できるようになった。仕様レビューでVariant必須の根拠を確認した。

## 判定

判定状態: bug-found（仕様レビューで期待値を確定し、Runnerの不一致を再現した）。
