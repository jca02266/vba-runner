---
id: EV-01000
evaluationRecordVersion: 2
candidateId: RCA-PRIVATE-VISIBILITY-010
campaign: FZ-COVERAGE-20260823
status: fixed
priority: 3
focus: クラス内Private UDT動的配列のReDim後要素アクセス
area: 宣言・型・リテラル
areaSource: confirmed
expectation:
  kind: spec
  statement: クラスモジュール内のPrivate Typeによる動的配列はReDim後にUDT要素として初期化され、メンバーを参照できる
  references:
    - source: ../../spec/MS-VBAL.txt
      ref: 5.2.3.1 variable declarations and resizable arrays; 5.2.3.3 User Defined Type Declarations
  verification: completed
findings:
  - BUG-00608
directCauseKey: class-body-type-not-parsed
causeKey: class-body-type-scope-missing
tests:
  - tests/spec/private-type-declaration.test.ts
directFixStatus: fixed
rootFixStatus: planned
commit: 'fe327385'
---

## 評価内容

クラス内Private Typeを要素型とする動的配列をReDimし、配列要素のUDTメンバーを初期化・参照する経路を確認する。

## 実施方法

```vba
' ClassCase.cls
Option Explicit
Private Type TItem
    Value As Long
End Type

Private items() As TItem

Public Function Probe() As Long
    ReDim items(0 To 1)
    items(0).Value = 21
    Probe = items(0).Value * 2
End Function

' Module1.bas
Option Explicit
Public Function Caller() As Long
    Dim instance As New ClassCase
    Caller = instance.Probe
End Function
```

`Caller`を実行し、ReDim後のUDT要素のメンバー代入と読み出しが成立するかを確認する。通常モジュールに同じ動的UDT配列を置く対照経路も実行する。

## 期待値

プログラム上の期待出力: `Caller=42`。

文章での期待: ReDimは宣言された動的配列の境界を設定し、各要素は宣言済みUDTとして既定値で生成されるため、Valueへの代入と読み出しが成立する。

## 結果

プログラム上の実測出力: 修正後のRunnerは`Caller=42`を返した。通常モジュール対照も42を返す。

文章での結果: クラス内TypeDeclarationをインスタンス環境へ登録し、ReDim後の固定・動的要素生成にもその環境を渡すことで、UDT型メタデータとメンバーアクセスが成立した。

## 判定

判定状態: fixed（クラス内Private UDT動的配列のReDimを回復し、回帰テストを通過）。

独立評価は同じ最小コードでError 424を再現し、通常モジュール対照が42になることを確認した。横展開ではParserの`parseClassBody`にType分岐がないこと、Evaluatorのクラス所有型登録がないこと、ReDimが既存メタデータに依存することを確認した。独立RCA提案と別エージェントの反証レビューはいずれも直接原因`class-body-type-not-parsed`と根因仮説RC-00122を支持したが、EventやPublic/Friend境界は未検証として切り分けた。RC-00122は未確定仮説である。
