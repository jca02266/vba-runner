---
id: RC-00014
procedureVersion: 1
status: under-review
causeKey: assignment-array-type-validation-bypass
statement: VBAの型付き配列全体代入禁止、Variant配列代入、要素代入の不変条件が共通代入入口に定義されず、識別子・Member・Setで局所的に判定されている
proposedBy: legacy-v1-reanalysis
originEvaluations:
  - EV-00388
  - EV-00389
evidence:
  - EV-00388は型付き配列識別子への全体代入でsource配列を保存した
  - EV-00389はClass/UDT Member経路で同じ全体代入を許可した
  - EV-00390のSet経路は関連証拠だが、Object参照分類RC-00001の起点として別管理する
  - 現行実装にはrejectTypedArrayWholeAssignmentという共有述語があるが、書込み入口ごとに呼出し契約が分散している
alternatives:
  - 単独の入口ガード欠落では識別子とMemberの同じalias結果を説明できない
  - Property Get配列の由来判定だけでは、typed array全体代入の禁止不変条件を説明できない
unresolved:
  - UDT/Object配列、Property Let内部保持、Setの配列ポリシーとの責務境界
  - Variant配列全体代入と要素代入のalias/コピー契約
  - rejectTypedArrayWholeAssignmentのIdentifier/Member/ImplicitWith以外のCallExpression、default property、Property Let、UDT、COM経路
  - RC-00001のObject参照分類およびRC-00015のRHS provenanceとの境界
  - 配列戻り値markerをvalidatorが消費するため、同じ値の再利用・複数setterで許可が一貫するか
reviews:
  - reviewer: rca-typed-assignment-review-2026-08-09
    verdict: supplement
    summary: UDTのInteger/String/Double/Date配列メンバーで異型whole-array代入とProperty ByRefがError 0で通過する。instantiateTypeがprimitive要素型メタデータを付与しないため、Class/Identifierとvalidator入力が分岐している。UDT Object配列要素のSet/代入検証欠落は別境界として残す
  - reviewer: rca-review-interface-set-2026-08-09
    verdict: confirm
    summary: UDTのprimitive配列メンバーはmetadata欠落だけでなくwhole-array validatorとelement coercionの共通経路を迂回する。Class/UDTのMemberExpression/CallExpressionで同じ欠落を再現し、COM/typed Object配列は未解決として分離する
  - reviewer: rca-object-array-member-review-2026-08-09
    verdict: supplement
    summary: UDT Object配列のLetスカラー検証は修正後に通るが、Set payload/NothingはevaluateSetStatementの汎用分岐で更新されない。Class配列との差は再現し、UDT Object配列のExcel契約とCOM/Collection境界は未確認
  - reviewer: legacy-root-challenge
    verdict: challenge
    summary: 共通不変条件の仮説は妥当だが、現行実装は複数入口で個別validatorを呼ぶため、共通入口が既に存在するという説明と混同してはならない
  - reviewer: legacy-root-challenge
    verdict: supplement
    summary: 左辺の宣言型・全体代入可否をRC-00014、右辺の配列由来をRC-00015、Object参照意味分類をRC-00001として二軸に分離する
  - reviewer: rc14-independent-review
    verdict: supplement
    summary: Identifier、Class/host Member、ImplicitWith、Setから共有validatorは呼ばれるため、真因は述語の不存在ではなく、allowArrayRebindやCallExpression・Property Let・COMなど入口ごとの呼出し契約が分散している点として精緻化すべきである
  - reviewer: rc14-independent-review
    verdict: challenge
    summary: validatorは左辺宣言型を直接判定せず、existing/valueのArray判定とprovenance markerに依存する。allowArrayRebindによる無条件バイパス、marker消費、Property/COM境界が未検証のため、共有入口で一度だけ判定できるというv1受入条件は未達である
acceptanceCriteria:
  - targetの宣言型とRHSの実行時categoryを共通入口で一度だけ判定する
  - typed array全体代入を拒否し、Variant配列・要素代入・正当なgetterを維持する
  - Identifier、Member、Set、Propertyの代表経路でaliasを発生させない
decision: 左辺ポリシーと右辺provenanceの責務境界、未網羅入口を追加調査するまでunder-reviewとして保持する
---

# 型付き配列全体代入ポリシーの分散

旧方式のEV-00388/389を中心に、型付き配列の全体代入禁止が入口ごとに分散した
設計上の真因を検証する。EV-00390は関連証拠だが、既存RC-00001との責務境界を確認する。

## 2026-08-09 収束チェックポイント

通常の評価キャンペーンに未処理のqueued候補がないため、次の候補取得ではなく、最優先の
未確定真因である本RC-00014の境界確認を行った。

`typed-array-root-cause`、`property-array-assignment`、`udt-object-array-set`、
`com-array-getter-assignment`は通過したが、CallExpressionのindexed Property/Dictionary、
既定Value Property Let、COM/Collection、Property内部保持、`allowArrayRebind`のwritebackを
網羅していない。したがってRC-00014はconfirmedへ遷移せず、これらの境界を最小テストで
追加確認することを次の優先作業とする。新規実装やROOTタスクの作成は、独立レビューで
unresolvedが空になった後に行う。

## 2026-08-09 収束チェックポイント（EV-00441後）

EV-00441ではMember/UDTのByRef `ReDim` writeback欠落を修正し、回帰テストで
Class・UDTの再束縛を確認した。ParamArrayのtyped-array全体代入はError 13で仕様どおり
だったが、ParamArray自身の`ReDim`禁止境界は別候補として残る。Property内部保持、
ImplicitWith、COM/host配列も未確認である。

次の優先候補は、実装領域の異なるCOM getter由来配列の要素category検証
（RCA-FOLLOWUP-028）とする。これは既存のMember修正で覆われないhost dispatch経路を
最小テストで判定でき、RC-00014の未解決境界を最も直接に減らす。

## 2026-08-09 収束チェックポイント（EV-00442後）

EV-00442ではopaque COM/host getter配列をObject()へ代入する際の全leaf検証を追加し、
Object/Nothing以外をError 13で拒否した。一方、独立レビューで同じopaque bypassが
Long()/String() destination、同型Object() Property Get、Property ByRef、
Collection/Dictionary method-return、多次元shapeに残ることを確認した。

次は同じ共通validatorのprimitive destination横展開（RCA-FOLLOWUP-029）を評価する。
Object()だけの局所修正で終わらせず、Long()/String()の異型要素を最小マトリクスで確認し、
同じ真因の範囲を確定する。

## 2026-08-09 収束チェックポイント（EV-00443後）

EV-00443ではopaque getter配列をLong()/String()へ代入するprimitive destinationの
無条件bypassを修正した。同型metadata付き配列は維持したが、独立RCAで同じ未検証受理が
ByRef配列引数、Property Let/Set、Collection/Dictionary item経路にも再現した。

次はこのbinder横展開（RCA-FOLLOWUP-030）を評価する。whole-assignment入口の修正だけで
は解消しない経路を分離し、Property内部保持とコンテナ経由の配列型契約を確認する。

## 2026-08-09 収束チェックポイント（EV-00444後）

EV-00444ではByRef、Property Let、Collection、Dictionaryの1次元opaque配列binderを
metadata必須化した。独立RCAの追加確認では、ParamArray経由でtyped binderへ再到達する
場合、CallByName VbLet、多次元primitive/Object配列は現修正後に仕様期待へ到達したが、
明示的な評価記録と回帰テストが不足している。

次の候補はParamArray relayとCallByName VbLetのopaque配列境界
（RCA-FOLLOWUP-031）とし、Variant packingとtyped binder再到達を同一マトリクスで確認する。
