---
id: RC-00105
procedureVersion: 1
status: hypothesis
proposedBy: evaluate-vba-runner
causeKey: typed-declaration-defaults-are-not-centralized
statement: Enumの宣言型をスカラーと配列の実行時型メタデータへ変換する責務が分離し、配列だけLong相当へ正規化されない
originEvaluations:
  - EV-00957
evidence:
  - tests/excel/queue/ExcelQueueVerification.result のXL-253
  - src/engine/evaluator.ts の配列要素メタデータ設定
confirmed:
  - ExcelのEnum配列はLong配列相当の8195/3を返す
  - 修正前runnerはEnum名の未知メタデータにより8204/5を返す
ruledOut:
  - UDT・Object・Variant配列の同一経路への誤帰属
unresolved:
  - ReDimとクラスフィールドのEnum配列
alternatives:
  - hypothesis: VarType組み込み関数だけのマッピング不足
    disposition: rejected
    reason: 配列初期化時点の要素メタデータがEnum名になっていることを確認した
reviews:
  - reviewer: udt_order_rca_review
    verdict: supplement
    summary: XL-253の直接バグは修正済みだが、Enum配列の型写像を共通化する設計原因は未解消で、ReDim・クラスフィールド・戻り値配列を追加検証する必要がある。
acceptanceCriteria:
  - Enum配列がLong配列相当のVarTypeを返す
  - Enum配列要素がLongのVarTypeと既定値0を返す
  - ReDimとクラスフィールドで同じ型契約を維持する
decision: 配列要素メタデータ生成を宣言型の共通写像へ統合し、未確認のReDim・クラスフィールドを別候補で検証する
---

# Enum配列型メタデータの写像

Enum配列だけがLong相当の型情報を失うため、スカラーEnum修正で導入した型判定を配列要素メタデータにも適用する仮説を登録する。
