---
id: RC-00028
procedureVersion: 1
status: hypothesis
causeKey: typed-array-parameter-element-type-validation-gap
statement: 配列引数の共通バインダーがコンテナの有無だけを検証し、宣言された要素型と実行時配列メタデータの一致を検証していない
proposedBy: rca-property-array-010
originEvaluations:
  - EV-00424
evidence:
  - ByRef Long()へInteger()を渡す標準ModuleとProperty Letの両方でError 13が発生せず、同じbindProcedureParameters経路を通過する
  - Array()由来のVariant配列もLong()引数へ通過する
  - validateArrayParameterContainerは従来Array.isArrayだけを確認していた
alternatives:
  - Property Letのディスパッチ固有不具合ではなく、標準Moduleでも再現するため共通バインダーの欠落である
  - ByRefの書戻しやクラス環境リークではなく、引数本体の型検証前に処理が通過している
unresolved:
  - 外部COM・ホスト由来で型メタデータを持たないSAFEARRAYの扱い
  - UDT/Object/Variant配列の要素型と暗黙変換の契約
  - Function/Property Getの配列戻り値とメタデータ保持の共通契約
followUpCandidates:
  - RCA-FOLLOWUP-011
  - RCA-FOLLOWUP-013
reviews:
  - reviewer: rca-typed-array-independent-review
    verdict: supplement
    summary: Function/Property GetとUDT配列でも既知の型一致・不一致は修正後に0/13となることを確認した。Object配列の共変性、メタデータを持たないCOM/ホスト配列、戻り値メタデータ保持は未確定であり、真因は支持するが確定条件は未充足とした
acceptanceCriteria:
  - 宣言型と既知の配列要素型が異なるByRef配列引数をError 13で拒否する
  - Property Letだけでなく標準Module、Function、Property Getの同一バインダー入口を検証する
  - 外部配列やUDT/Object配列の未確定契約を別候補として記録し、既知の正当な配列受理を壊さない
decision: 共通配列引数バインダーの要素型検証欠落を真因仮説として登録する。独立レビューと未解決経路の検証が完了するまで確定しない
---

# Typed array parameter element-type validation gap

配列引数の型検証が配列コンテナの存在確認に留まり、宣言された要素型と実際の配列の型・由来を比較していない。
