---
id: RC-00104
procedureVersion: 1
status: hypothesis
proposedBy: evaluate-vba-runner
causeKey: typed-declaration-defaults-are-not-centralized
statement: Enumを含む宣言型の型メタデータ登録と既定値実体化が別経路で実装され、EnumのLong既定値がNothingとして保存される
originEvaluations:
  - EV-00956
directCause: Enum型をLongとして記録しても、初期値決定でEnumを数値型として扱わずvbaNothingを保存する
designCause: 宣言型から既定値を導く責務が中央化されていない
confirmed:
  - ExcelはEnum変数の既定値を0としてCStr変換する
  - vba-runnerは同じ変数でVarType=3とError 91を返す
evidence:
  - tests/excel/queue/ExcelQueueVerification.result のXL-252
  - src/engine/evaluator.ts のEnum型メタデータ登録と初期値分岐
alternatives:
  - hypothesis: CStrだけがEnum値を変換できない
    disposition: unresolved
    reason: 既定値自体が0として保存されているかを先に確認する必要がある
unresolved:
  - Enum配列の要素メタデータ契約
decision: EnumをLong相当の値型として既定値0へ具体化する共通初期値経路を適用し、配列要素メタデータは別候補で横展開する
reviews:
  - reviewer: udt_order_rca_review
    verdict: challenge
    summary: スカラーEnumの修正は確認できたが、Enum配列・追加ロード経路・モジュール間Private Enum可視性は未解決であり、共通初期値契約の確定には追加評価が必要
acceptanceCriteria:
  - Enum型モジュール変数の既定値が0になりCStrで0を返す
  - Enum配列とプロシージャローカルEnumも同じ既定値契約になる
---

# Enum型の既定値実体化

## 対処状況

Excel実機との差分を確認し、真因仮説と回帰テストを登録した。実装修正と独立確認は未完了である。
