---
id: RC-00004
procedureVersion: 1
status: confirmed
causeKey: mkdir-existing-path-validation
statement: FileSystem backendのmkdir意味をVBAの単一階層作成契約へ変換する責務が入口に定義されず、MkDirがrecursive host semanticsへ委譲して既存・親不在のエラー境界を失っていた
proposedBy: ev00339-root-analysis
originEvaluations:
  - EV-00339
evidence:
  - 修正前のMkDirはrecursive mkdirへ委譲し、既存ディレクトリを成功、欠落親を自動作成していた
  - RmDir、Open、FileCopy、FSO CreateFolder、CopyFolder、MoveFolderを横展開し、契約が異なるか既に入口検証済みであることを確認した
  - MkDirとFSO CreateFolderへ共通の単一階層検証を適用し、Node/Memory backend差を入口で吸収した
alternatives:
  - recursive:trueの単純なオプション誤りだけでは、VBAのError 75/76写像責務が欠けた理由を説明できない
  - sandbox path resolverの誤りでは、正しいパスでも既存・親不在状態で再現したことを説明できない
  - 全filesystem APIの単一共通原因では、削除・コピー・ファイル作成の異なる契約を説明できない
unresolved: []
reviews:
  - reviewer: multi-ev-io-review
    verdict: supplement
    summary: backend依存のmkdir契約欠落とMkDir固有の単一階層契約を分離し、横展開対象と除外対象を列挙した
  - reviewer: root-cause-implementation-review
    verdict: confirm
    summary: 共通検証をMkDirとFSO CreateFolderへ適用し、既存・親不在境界の回帰と他APIの契約分離を確認した
acceptanceCriteria:
  - MkDirの既存パスはError 75、直上親不在はError 76になる
  - FSO CreateFolderの既存パスはError 58、直上親不在はError 76になる
  - Node/Memory backendのmkdir挙動に依存せず、recursive作成をユーザー指定パスへ適用しない
  - RmDir、Open、FileCopy、CopyFolder、MoveFolder、DeleteFolderの既存契約を変更しない
  - EV-00339とfilesystem-extraの回帰が通過する
decision: ROOT-00004でbackend委譲前の単一階層ディレクトリ検証を共通化し、MkDirとFSO CreateFolderへ適用する
---

# MkDirのVBAパス契約欠落

EV-00339をROOT_CAUSE_ANALYSIS v1で再分析し、host filesystemのrecursive semanticsをVBA意味層で吸収していなかったことを真因として確定した。
