---
id: RC-00124
status: hypothesis
procedureVersion: 1
scope: Public Module.Type UDT配列のByRef引数型識別
proposedBy: qualified_udt_array_rca
originEvaluations:
  - EV-01011
directCauseKey: qualified-udt-array-element-identity-mismatch
statement: 同じPublic UDTを指す修飾名と裸名を配列要素型の文字列比較で区別し、ByRef配列引数を誤ってType mismatchにする
causeKey: type-identity-canonicalization-missing
evidence:
  - src/engine/evaluator.ts: validateArrayParameterContainer compares runtime and declared element metadata
  - EV-01011: Producer.RecordT配列をProducer.Sum(values)へ渡すとError 13
confirmed:
  - ParserはModule.Type配列宣言を受理する
  - Public UDTの配列実体化後にByRef配列コンテナ検査へ到達する
  - 修飾名と裸名の型メタデータ比較でType mismatchが発生する
ruledOut:
  - UDT宣言の構文解析失敗
  - 配列実体化そのものの失敗
  - 非配列値のByRef受け渡し
unresolved:
  - runtime metadataのcanonical identity不足か、単純な修飾名不一致か
  - Property Let、Function/Property配列戻り値、ReDim後の同一経路
  - 同名UDTのモジュール優先順位とPrivate型境界
alternatives:
  - alternative: すべての型名を裸名へ比較前に切り詰める
    reason: 同名UDTを異なる所有者から誤って同一視するため、所有モジュールを含む識別子が必要
decision: canonical UDT identityを生成・保持し、配列ByRef検査など全入口で共有する対処候補として登録する
acceptanceCriteria:
  - 同じPublic UDTのbare/qualified配列をByRefで相互に受け渡し、要素書戻しを維持する
  - 異なる所有モジュールの同名UDTはType mismatchとして分離する
  - 固定/動的配列、ReDim Preserve、Erase、Property Let、配列戻り値でidentityを維持する
  - Private UDTの越境拒否と既存のプリミティブ配列型検査を維持する
reviews:
  - reviewer: qualified_udt_array_rca_challenge
    verdict: supplement
    summary: 配列コンテナ検査の文字列比較が直接原因候補であることを支持し、Property・戻り値・ReDimの追加確認を要求
---

この記録は独立提案と補足レビュー後も、未解決経路が残る仮説である。
