---
id: RC-00108
procedureVersion: 1
status: confirmed
proposedBy: evaluate-vba-runner
causeKey: auto-new-state-is-keyed-only-by-variable-name
statement: As New の遅延インスタンス状態を Evaluator 全体の変数名 Map で管理し、所有 Environment から分離していた
originEvaluations:
  - EV-00962
evidence:
  - src/engine/evaluator.ts の autoInstanceVars
  - tests/spec/as-new-module-scope.test.ts
confirmed:
  - 同名 Private item のモジュールロード順で Set item = Nothing 後の Err.Number が 13 または 0 に変化する
  - モジュール単位の Environment に状態を分離すると両ロード順で 0 になる
ruledOut:
  - 初回の As New 遅延参照そのもののクラス解決
  - 異なる変数名のモジュール間汚染
  - クラスフィールドおよび As New 配列要素の同一 Map 経路
unresolved: []
alternatives:
  - hypothesis: クラス型名の解決または型変換が原因
    disposition: rejected
    reason: Map の登録順だけで Set Nothing 後の再生成結果が変化する
reviews:
  - reviewer: eval_as_new_quick
    verdict: confirm
    summary: Environment 所有の状態分離が必要であり、共有Mapの変数名キーがロード順依存を生むことを確認
  - reviewer: udt_order_rca_review
    verdict: supplement
    summary: 共有 Map の変数名キーが直接原因で、初回参照ではなく再生成経路に限定される
decision: 所有 Environment ごとの As New 状態管理へ変更し、同名 Private モジュールのロード順回帰を追加した
acceptanceCriteria:
  - 同名Private As New変数のロード順を入れ替えても再生成結果が一致する
  - 遅延状態が変数を所有するEnvironment単位で分離される
---

As New の自動生成状態が Evaluator 全体の Map に変数名だけで登録され、同名 Private 変数の後続宣言で上書きされていた。Set Nothing 後の再生成が誤ったクラス型を使い、ロード順依存の Error 13 を生じていた。
