---
id: RC-00002
procedureVersion: 1
status: confirmed
causeKey: class-field-environment-leak
statement: クラスinstanceEnvをcall-site Environmentの子として生成し、クラス固有フィールドのown操作と通常の親スコープ解決を分離していないため、読み書き・型情報・WithEvents判定が呼出元状態へ漏れる
proposedBy: ev00399-root-analysis
originEvaluations:
  - EV-00399
evidence:
  - evaluateSetStatementと通常代入のクラスメンバー判定はinstanceEnvのget経路を通る
  - EV-00399ではcallerの同名typed arrayがクラスフィールドとして扱われ、Property Let本体へ到達しなかった
  - hasOwnVariableでinstanceEnv固有フィールドに限定すると同じProperty Let配列引数が受理された
  - implicitWith配列代入、Erase、Property Get/Method配列fallbackに親チェーン直結getが残る
  - createInstanceFromDefがinstanceEnvの親に動的なcaller Environmentを渡している
  - Class Boxの未定義Probeがcallerローカル値を返し、未知Member代入がcaller変数を書き換えた
  - caller同名配列がclass method内のReDimで変更され、module-level Public値は引き続き参照できた
alternatives:
  - Property LetのByRef配列引数自体を拒否する仕様差では、callerの同名変数の有無で結果が変わることを説明できない
  - 配列型validatorの共通化不足だけでは、setter呼出し前のクラスフィールド探索が親Environmentを参照したことを説明できない
unresolved: []
reviews:
  - reviewer: ev399-root-review
    verdict: challenge
    summary: getClassFieldへ移行済みでない3つの直結instanceEnv.get経路が残り、横展開を完了するまで確定できない
  - reviewer: ev399-root-review-followup
    verdict: challenge
    summary: instanceEnv.set/isWithEventsの親書込みと、呼出元envを親にするcreateInstanceFromDef自体がより深い設計原因候補になる
  - reviewer: ev399-scope-audit
    verdict: supplement
    summary: 3つの直結getとEraseの親書込みがcaller同名状態を漏らすことを確認し、通常Identifierの意図的親参照とは責務が異なると整理した
  - reviewer: ev399-parent-scope
    verdict: challenge
    summary: createInstanceFromDefがcall-site envを親にする設計自体とown/chained API未分離が根本候補で、nested classを含む追加境界が未解決と指摘した
  - reviewer: ev399-final-review
    verdict: challenge
    summary: read/write/type metadata/event ownershipの全経路とCallByName・nested・WithEvents境界が未検証で、ROOTタスク化はまだ早いと確認した
  - reviewer: remediation-verification
    verdict: confirm
    summary: instanceEnvの親を安定したmodule/global環境へ分離し、own scopeの書込み・配列型情報・WithEvents判定へ変更した後、caller漏れと関連経路の回帰が解消した
acceptanceCriteria:
  - callerとinstanceEnvに同名の配列・スカラー・オブジェクトがあるProperty Let/Set境界を分類する
  - クラスフィールド探索がinstanceEnv固有変数と親スコープ参照を用途別に区別する
  - Property Letの暗黙ByRef配列引数がcaller同名変数の有無にかかわらずsetter本体へ到達する
  - Member、With、Set、Property Let/Setの代表経路で既存の名前解決と回帰を維持する
  - caller同名の配列・スカラー・WithEvents変数がMember、With、Set、Property Let/Set、Erase、ReDim、配列読み書きへ漏れない
  - builtins CallByNameのVbGet/VbLet/VbSetがcaller状態へ漏れない
  - 未知クラスメンバーがcaller変数へフォールスルーせず適切なエラーになる
  - createInstanceFromDefの親Environmentをclass module/globalへ固定する必要性を確認し、module/global境界を壊さない
  - nested class、COM、Collection、WithEventsの既存挙動を維持する
decision: ROOT-00002でclass instance scope parentとown/chained APIの責務境界を分離し、Member、With、Set、Property、Erase、ReDim、CallByName、nested、WithEventsの回帰を通過した
---

# EV-00399 Property Letのクラスフィールド境界

旧方式で確認済みだった`class-field-environment-leak`を、新方式v1の仮説として
再分析する。旧評価は起点情報であり、この記録の独立レビューを代用しない。
