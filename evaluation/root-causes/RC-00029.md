---
id: RC-00029
procedureVersion: 1
status: under-review
causeKey: property-paramarray-value-tail-model
statement: Property Let/SetのParamArrayと、ParamArrayへ含めない代入RHSのvalue-paramを同じ引数列として検証・束縛している
proposedBy: rca-followup-015-independent-evaluator
originEvaluations:
  - EV-00429
evidence:
  - MS-VBAL §5.3.1.7はProperty Let/SetのParamArray後にvalue-paramを許し、value-paramをParamArrayから除外する
  - parser.tsはParamArray後のProperty Let/Set value-paramを無条件に拒否していた
  - runtimeのbinderもParamArray後のraw引数とRHSを別slotとして表現していなかった
alternatives:
  - 通常のSub/FunctionまたはProperty GetにもParamArray後続を許可すべきという解釈
  - As Variant/Objectのlexer分類が宣言拒否の原因であるという仮説
unresolved:
  - Interface/COMのProperty Let/Setで同じvalue-tail契約を適用する範囲
  - Property Let/Setの名前付きvalue、ByRef式、0個ParamArray要素の実Excel境界
reviews:
  - reviewer: rca-review-001
    verdict: challenge
    summary: class-dispatch-omits-paramarray-tailでは宣言時構文エラーを説明できず、Property value-tailの文法・意味境界として別原因へ分離すべき
  - reviewer: eval-interface-com-016
    verdict: supplement
    summary: 修正後のクラスProperty、CallByName、外部dynamic prototypeで0/1/複数index、名前付きRHS、ByRefを確認し、再発なし。宣言型を持たない外部COM相当のindexed putだけは別契約として未解決
  - reviewer: rca-interface-callbyname-2026-08-09
    verdict: supplement
    summary: Interface参照のCallByName VbLet/VbSetだけがError 438となる最小再現を確認した。通常Interface assignmentとClass/CallByName直接経路は正常で、CallByNameのInterface dispatch欠落を同じvalue-tail設計真因の追加経路として支持する
acceptanceCriteria:
  - Property Let/SetのParamArray+value宣言を解析できる
  - 直接代入とCallByNameのLet/SetでRHSをParamArrayへ含めず束縛できる
  - ParamArray要素のByRef書戻しを維持する
  - 通常Sub/FunctionとProperty GetのParamArray末尾制約を緩めない
decision: 直接のProperty value-tail文法・束縛欠落は修正済みで、Interface参照のCallByName dispatch欠落も同じ共通入口へ統合する。外部COM descriptorの型メタデータ契約は別境界として未確定のため、真因はunder-reviewのまま保持する
---

# Property Let/SetのParamArray value-tail

RC-00013のClass ParamArray tail調査から、Property Let/Setだけが持つvalue-paramを
ParamArrayと分離できない文法・束縛モデルの欠落を切り出した。
