---
id: RC-00103
procedureVersion: 1
status: hypothesis
proposedBy: udt_order_rca
causeKey: declaration-registration-order-couples-runtime-initialization
statement: モジュール宣言の型登録と実行時変数初期化を同じ逐次走査で行うため、型付き変数の意味が宣言およびモジュールのロード順に依存する
originEvaluations:
  - EV-00955
directCause: 型レジストリ完成前にモジュール変数を初期化し、未登録UDT型をNothingとして保存した後も再初期化しない
designCause: 型名の束縛を完了させるフェーズと、宣言型の既定値を具体化するフェーズの所有責務が分離されていない
confirmed:
  - 後置UDT宣言はvba-runnerでError 91、前置宣言は0:42になる
  - Excel実機は前置・後置とも0:42になる
evidence:
  - spec/MS-VBAL.txt §5.2、§5.2.3、§5.6.16.7の独立レビュー
  - tests/excel/queue/ExcelQueueVerification.result のXL-251
alternatives:
  - hypothesis: MemberExpressionのNothing検査が誤っている
    disposition: rejected
    reason: UDT宣言を前置すると同じメンバーアクセスが成功する
  - hypothesis: Parserが後置UDT宣言を欠落させる
    disposition: rejected
    reason: TypeDeclarationは後段で登録され、手続きローカル変数から利用できる
unresolved:
  - Enum、As New、Private Type可視性など隣接経路の実装境界
decision: 独立レビュー完了後、全モジュールの型宣言登録を完了してからモジュール変数を具体化する共通フェーズを対処候補とする
acceptanceCriteria:
  - UDT宣言の前後でスカラー変数の初期値とメンバーアクセス結果が一致する
  - 動的UDT配列が宣言順にかかわらずUDT要素を生成する
  - プロシージャローカル変数・組み込み型・固定長配列の既存挙動を維持する
reviews:
  - reviewer: udt_order_rca_review
    verdict: challenge
    summary: UDT経路の中心原因は支持するが、Enum・As New・モジュール境界は同一修正で扱わず別途検証する
---

# モジュール宣言登録と変数初期化の分離

## 対処状況

Excel実機で後置UDT宣言が有効であることを確認し、vba-runnerの互換性バグとして記録した。共通フェーズへの実装と回帰テストは未実施である。
